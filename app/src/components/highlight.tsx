import { styled } from "../../styled-system/jsx";

export const Highlight = styled("div", {
  base: {
    position: "absolute",
    backgroundColor: "pink.500",
    opacity: "0.5",
    pointerEvents: "none",
  },
});
