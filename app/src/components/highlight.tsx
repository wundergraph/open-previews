import { styled } from "../../styled-system/jsx";

export const Highlight = styled("div", {
  base: {
    position: "fixed",
    backgroundColor: "pink.100",
    opacity: "0.5",
    pointerEvents: "none",
  },
});
